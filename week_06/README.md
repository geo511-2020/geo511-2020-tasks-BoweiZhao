In this week, I learned lots of new useful functions in r.  

na.rm=T:This code means we will exclude missing values from analysis.  

st_set_geometry(NULL):This code means we will drop the geometry column.